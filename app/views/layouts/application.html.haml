!!!5
%html
  %head
    %meta{:charset => "utf-8"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    %title= full_title(yield(:title))
    %meta{:content => "An Application for the Austin Center for Design", :name => "description"}
    %meta{:content => "Scott Gerlach", :name => "author"}
    = stylesheet_link_tag "application", :media => "all"

    = csrf_meta_tags
    = yield(:head)

  %body
    %header
      = render 'layouts/navigation'

    #main{:role => "main"}
      .container
        .content
          .row
            .span12
              = yield

    %footer
    = javascript_include_tag "application"
